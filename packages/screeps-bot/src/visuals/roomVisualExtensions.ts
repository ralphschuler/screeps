/**
 * Bot compatibility export for RoomVisual prototype extensions.
 *
 * Importing this module evaluates the framework implementation and installs the
 * extensions. Keep bot code thin; implementation lives in `@ralphschuler/screeps-visuals`.
 */

export { initializeRoomVisualExtensions } from "@ralphschuler/screeps-visuals";
