/**
 * Type definitions for @ralphschuler/screeps-intershard
 */

// Re-export types from schema
export type {
  ShardRole,
  ShardHealthMetrics,
  ShardState,
  PortalInfo,
  InterShardTask,
  InterShardMemorySchema
} from './schema';
