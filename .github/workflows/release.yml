name: Release

on:
  push:
    branches:
      - main

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    defaults:
      run:
        working-directory: packages/screeps-bot
    env:
      CI: true
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: packages/screeps-bot/.nvmrc
          cache: npm

      - name: Install dependencies for screeps-spawn
        working-directory: packages/screeps-spawn
        run: npm ci --ignore-scripts

      - name: Build screeps-spawn
        working-directory: packages/screeps-spawn
        run: npm run build

      - name: Install dependencies for screeps-chemistry
        working-directory: packages/screeps-chemistry
        run: npm ci --ignore-scripts

      - name: Build screeps-chemistry
        working-directory: packages/screeps-chemistry
        run: npm run build

      - name: Install dependencies for screeps-utils
        working-directory: packages/screeps-utils
        run: npm ci --ignore-scripts

      - name: Build screeps-utils
        working-directory: packages/screeps-utils
        run: npm run build

      - name: Install dependencies for screeps-defense
        working-directory: packages/screeps-defense
        run: npm ci --ignore-scripts

      - name: Build screeps-defense
        working-directory: packages/screeps-defense
        run: npm run build

      - name: Install dependencies for screeps-economy
        working-directory: packages/screeps-economy
        run: npm ci --ignore-scripts

      - name: Build screeps-economy
        working-directory: packages/screeps-economy
        run: npm run build

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Build
        run: npm run build

      - name: Configure Git
        working-directory: .
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit build files to main
        working-directory: .
        run: |
          # Verify we're on the main branch
          CURRENT_BRANCH=$(git symbolic-ref --short HEAD)
          if [ "$CURRENT_BRANCH" != "main" ]; then
            echo "Error: Not on main branch (current: $CURRENT_BRANCH)"
            exit 1
          fi
          
          git add packages/screeps-bot/dist/
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: update build files [skip ci]"
            if ! git push origin main; then
              echo "Error: Failed to push to main. There may be conflicts or permission issues."
              exit 1
            fi
            echo "Successfully pushed build files to main"
          fi

      - name: Archive build output
        run: tar -czf dist.tar.gz dist

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.run_number }}
          name: Release ${{ github.run_number }}
          generate_release_notes: true
          files: |
            packages/screeps-bot/dist/**
            packages/screeps-bot/dist.tar.gz
          make_latest: true
          draft: false
          prerelease: false
          allow_updates: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
