name: Screeps InfluxDB Exporter CI

on:
  push:
    paths:
      - 'packages/screeps-influx-exporter/**'
  pull_request:
    paths:
      - 'packages/screeps-influx-exporter/**'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/screeps-influx-exporter
    env:
      CI: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: packages/screeps-influx-exporter/.nvmrc
          cache: npm
          cache-dependency-path: packages/screeps-influx-exporter/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build TypeScript
        run: npm run build

      - name: Validate Docker build
        uses: docker/build-push-action@v5
        with:
          context: packages/screeps-influx-exporter
          push: false
          load: true
          tags: screeps-influx-exporter:ci
