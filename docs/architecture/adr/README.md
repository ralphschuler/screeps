# Architecture decision records

Canonical ADRs currently live in [`docs/adr/`](../../adr/README.md).

This directory exists to preserve the current documentation hierarchy while avoiding duplicate ADR content. Add new ADR files to `docs/adr/` unless the repository completes a future physical move of the ADR tree.
